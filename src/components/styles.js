import styled from "styled-components";

const Header = styled.h1`

`

const Paragraph = styled.p`

`

const Button = styled.button`

`

const Input = styled.input`
    height:100%;

`

export {Header, Paragraph, Button, Input}